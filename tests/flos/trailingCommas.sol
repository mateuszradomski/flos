contract TrailingCommas {
    function test() {
        (v0, v1, state.preimageOffset, state.memRoot,,) = sys.handleSysRead(args);
    }
}
// Above input, below output
contract TrailingCommas {
    function test() {
        (v0, v1, state.preimageOffset, state.memRoot,,) = sys.handleSysRead(args);
    }
}
